TUNE_FLAGS="-march=armv7-a -mfpu=neon-fp16 -mfp16-format=ieee -mfloat-abi=hard"
CFLAGS="${TUNE_FLAGS} -O2 -pipe -fno-diagnostics-color"
CXXFLAGS="${CFLAGS}"
CHOST="armv7a-hardfloat-linux-musleabi"

MAKEOPTS="-j5"

PAX_MARKINGS="XT"
